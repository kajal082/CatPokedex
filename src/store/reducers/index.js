const initialState = {
  loading: false,
};
const Reducer = (state = initialState, action) => {
  switch (action.type) {
    // case :

    default:
      return state;
  }
};

export default Reducer;
